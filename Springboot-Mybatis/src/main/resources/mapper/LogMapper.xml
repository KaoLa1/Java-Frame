<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD com.example.Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.LogMapper">

 <!--   <resultMap id="logMap" type="Log">
        <result column="old_id" jdbcType="VARCHAR" property="old_id"/>
        <result column="new_id" jdbcType="VARCHAR" property="new_id"/>
        <result column="delete_flag" jdbcType="VARCHAR" property="delete_flag"/>
    </resultMap>-->

    <select id="getId" resultType="map">
        select new_id,old_id from meta_middle_syncid_log where  delete_flag = 0
    </select>

</mapper>